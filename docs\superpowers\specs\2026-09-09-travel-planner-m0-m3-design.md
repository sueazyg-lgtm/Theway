# 旅行微信小程序 M0-M3 设计

## 1. 目标与边界

本项目交付一套可导入微信开发者工具的原生微信小程序工程。用户可以建立一趟旅行，维护参与人、车辆、逐日路线、活动、交通、住宿、预算、预订条件、实际账单、退款、分摊与结算，并通过方案预览处理行程变化。

正式品牌尚未确定。代码使用中性代号 `travel-planner`，产品标题、色彩、图标和文案通过独立配置层替换，不把代号当作正式名称。

首轮实现不依赖地图、酒店、票务、支付或消息推送接口。地点、路线耗时、价格和条件支持手工录入。没有微信云环境时，应用进入带明显标识的隔离演示模式；本地数据只能证明单机持久化，不能宣称已实现跨设备共享。

## 2. 已确认技术方案

采用微信原生小程序与 TypeScript。领域计算保持为不依赖微信运行时的纯 TypeScript 模块，由 Vitest 在 Node.js 中独立测试。页面通过应用服务调用 Repository 接口，不直接读写本地缓存或云数据库。

数据访问提供两种适配器：

- `DemoLocalRepository`：使用微信本地存储，面向无配置的单机演示、开发和回归测试。
- `CloudRepository`：面向微信云开发的数据仓储与身份校验。只有配置有效云环境后才启用；真实模式不回退到绕过鉴权的本地写入。

外部地图能力通过 `MapProvider` 接口封装为地点搜索、路线估算和打开导航。未配置时，地点文本、距离、最小/最大耗时由用户手工维护。

## 3. 工程结构

```text
travel-planner/
├─ miniprogram/
│  ├─ app.ts / app.json / app.wxss
│  ├─ config/                 # 代号、主题 token、运行模式
│  ├─ pages/                  # 旅行列表和五个旅行内入口
│  ├─ components/             # 状态、金额、时间线、表单等复用组件
│  └─ adapters/               # 页面事件到应用服务的薄适配
├─ domain/
│  ├─ models/                 # 领域实体、枚举和值对象
│  ├─ calculations/           # 时间、预算、分摊、退款、结算、优惠
│  ├─ rules/                  # 冲突与信息完整性检查
│  └─ services/               # 变更预览与规划快照
├─ services/
│  ├─ repositories/           # Repository 契约
│  ├─ demo-local/             # 本地持久化实现
│  ├─ cloud/                  # 云开发实现与鉴权边界
│  └─ map/                    # 手动与真实地图适配器
├─ cloudfunctions/            # 云端写入、分享、权限和幂等操作
├─ fixtures/
│  ├─ generic/                # 第10章虚构算例
│  └─ xinjiang/               # 新疆 seed、来源、冲突与验收期望
├─ tests/                     # 领域、仓储契约、案例和验收测试
├─ docs/                      # 决策、需求映射、运行和限制
├─ package.json
├─ tsconfig.json
└─ project.config.json
```

每个领域文件保持单一职责。页面只做展示、输入校验和状态反馈；金额、时间与冲突结论不能只存在于页面事件函数中。

## 4. 十三个功能模块与五个入口

十三模块口径如下：

1. 旅行建立与基础设置。
2. 参与人、分组与车辆。
3. 路线、旅行日与规划版本。
4. 每日活动与时间线。
5. 地点、交通、停车与补给。
6. 住宿与逐晚房间。
7. 预订、出行条件与待办。
8. 预算与优惠比较。
9. 实际账本与退款。
10. 分摊与结算。
11. 冲突检查、变更预览与方案切换。
12. 分享、权限与版本冲突。
13. 归档、审计与来源追溯。

旅行列表位于旅行外层。进入旅行后维持五个稳定入口：

- 旅行总览：模块 1-3 的摘要，并提供分享、风险、预算和归档入口。
- 行程：模块 3-6，按日期组织活动、交通、停车和住宿。
- 预算：模块 8-10，分别展示预算、实际、待确认和个人承担。
- 预订与待办：模块 7，购买、预约和使用状态互相独立。
- 方案调整：模块 11，联动模块 3-10；模块 12-13 横跨所有入口。

## 5. 领域模型

所有业务对象使用稳定字符串 ID、`createdAt`、`updatedAt` 和整数 `revision`。旅行内对象包含 `tripId`；规划对象另含 `planId`。日期使用 `YYYY-MM-DD`，完整时间使用含时区的 ISO 8601 字符串。金额使用人民币整数分。

主要实体：

- 身份与旅行：`User`、`Trip`、`TripAccess`、`Participant`、`Group`。
- 规划：`PlanVersion`、`TripDay`、`Activity`、`TransportLeg`、`Place`、`Vehicle`、`VehicleAssignment`、`ParkingSession`。
- 住宿与预订：`Lodging`、`RoomNight`、`Booking`、`BookingItem`、`TravelCondition`、`Task`。
- 财务：`BudgetItem`、`Expense`、`ExpenseLine`、`Refund`、`SettlementTransfer`、`DiscountOption`。
- 变更与来源：`ChangePreview`、`InformationSource`、`ImportRecord`、`ConflictRecord`、`AuditLog`、`ShareGrant`。

规划版本保存可变计划；`Expense`、`Refund` 和已确认的 `SettlementTransfer` 是旅行级事实，不随方案切换、恢复或删除而回滚。预订是现实状态记录，规划版本只持有引用。

费用覆盖状态明确区分：金额未知、未计入、已包含、确定免费、不适用。未知金额使用空值，不使用零。

## 6. 核心计算

### 6.1 时间

时间线按活动、交通、接驳、排队、缓冲的独立字段计算，防止重复累计。最小和最大耗时形成到达窗口：

- 整个窗口早于截止：按当前估算可满足。
- 到达窗口跨过截止：可能迟到。
- 最早到达也晚于截止：按当前估算冲突。
- 任一关键耗时未知：信息不足。

固定时间不自动后移；前序迟到以分钟差显示。跨午夜使用完整日期时间计算。车辆位置从停车事件持续到取车事件；车辆不在自驾段起点时产生冲突。

### 6.2 预算和实际账本

预算汇总分别计算已知下限与上限，同时返回未知条目清单。预算、预订和实际支付是不同对象：

- 已预订或已购买不会创建 `Expense`。
- 只有已确认支付进入实际支出。
- 只有已确认到账退款冲减实际支出。
- 结算转账不计入旅行消费。
- 订金通过已发生金额和剩余预计金额表达，避免与总预算重复累计。

### 6.3 分摊

支持按人均分、权重、固定金额、房间入住人员和两级分组快照。最终叶子始终是 `Participant`，每人每笔费用只出现一次。

比例分摊先向下取整到分，剩余分按小数余数降序分配；余数相同时按稳定 participantId 排序。固定分配不守恒时禁止确认。实际账单确认后保存分摊快照，人员改名或分组变化不重算历史。

### 6.4 退款和结算

退款必须关联原账单，累计金额不得超过已确认支付。默认按原分摊比例冲减，也允许在金额守恒时指定。

余额计算：

```text
净垫付 = 已确认支付 - 已确认收到退款
净承担 = 已确认分摊消费 - 已分配退款冲减
余额 = 净垫付 - 净承担 + 已确认结算转出 - 已确认结算转入
```

余额为正表示应收，为负表示应付，总和必须为零。转账建议按稳定 ID 顺序配对，不承诺最少笔数。建议与实际转账分开，只有已确认转账影响剩余余额。

## 7. 持久化与一致性

Repository 以完整旅行聚合和原子命令为边界。所有写命令携带 `expectedRevision`；不匹配时返回 `version_conflict`，页面保留表单并提示刷新或重新预览。

财务写入携带 `requestId`。同一请求重放返回原结果，不创建第二笔账。关键关联写入在云端事务中执行；本地适配器通过单次聚合替换模拟同一原子边界。

本地存储保存 schema 版本。迁移失败时保留原数据并进入只读恢复状态，不覆盖旧值。每次正常保存更新“最后本地保存时间”；只有云仓储成功后才显示“最后同步时间”。

## 8. 新疆种子数据与冲突

新疆案例位于独立 fixture，不进入默认值或通用枚举。导入记录包含原文片段、章节、值、单位、确定性、来源声明核实时间、导入时间、方案范围和冲突组。

种子数据包括 10 天、9 晚、3 位占位参与人、2 个组和 1 辆案例车辆。它们只是测试输入，应用允许任意人数、目的地和交通方式。

案例状态遵循以下规则：

- 酒店“已预订”和门票“已购买”保留为预订状态，不生成实付。
- 付款人、退款和转账为空，因此初始正式账本及结算为空。
- 当前线、原后半程和天气替代线是独立 `PlanVersion`。
- C01-C10 作为 `ConflictRecord` 展示，不用后出现的文字覆盖先出现的候选值。
- 原攻略“可行”作为来源文字保留，系统根据结构化数据独立输出冲突或信息不足。
- 10 月 2 日和 3 日组房价分配标记为推导；双人组内部承担未确认时不生成正式个人结算。
- 新疆 X01-X20 与通用 F01-F10、T01-T08 分开报告。

## 9. 页面与交互

首轮视觉采用中性、清晰、移动端优先的 token。品牌配置替换不影响领域数据和页面结构。

所有核心页面提供加载、空数据、失败、权限不足和正常状态。保存时禁用重复提交；失败保留输入。重要状态同时使用文字、图标和颜色。金额页分区展示预算已知部分、待估价项、已确认实际、待确认账单和个人承担。

删除存在引用的对象时展示影响范围。规划数据优先取消或停用；实际账单、退款、结算和审计记录不可被方案删除连带移除。

方案变更先创建 `ChangePreview`，展示时间、费用、预订和冲突差异。用户应用后才创建新规划版本；基线 revision 变化时拒绝旧预览。

## 10. 权限与分享

参与人不等于登录用户。未绑定用户的儿童或亲友仍可参与房间和费用计算。

所有者管理全部规划和账目；同行成员只查看获准内容并提交待确认支出；只读访客只看授权摘要。知道 tripId 不构成权限。

分享凭证由云端校验有效期、撤销状态和可见范围。默认隐藏订单号、联系方式、附件和他人的个人结算。隔离演示模式只演示权限过滤结果，不提供可对外访问的真实链接，并明确标记“本机演示”。

## 11. 错误处理

服务统一返回：`validation_error`、`forbidden`、`not_found`、`version_conflict`、`external_unavailable` 和 `storage_error`。页面将其映射为可操作的中文说明。

地图、云环境或外部接口不可用时，不伪造成功：地图回退到手工录入；云端保存失败保留表单；分享显示不可用原因；网络失败允许用户主动重试。首版不建立断网财务提交队列，避免重复记账。

## 12. 测试设计

开发遵循红—绿—重构。每个新增核心函数先写失败测试并确认失败原因，再写最小实现。

测试分层：

- 单元测试：F01-F10、T01-T08，以及金额、时间、车辆位置、状态机和错误分支。
- Repository 契约测试：本地与云适配器遵守相同读写、版本和幂等语义。
- 新疆案例测试：X01-X20，区分原文提取一致性、计算正确性和外部事实待核实。
- 页面状态测试：空数据、失败、权限、重复提交、表单保留和五入口联动。
- 微信环境验收：开发者工具中导入、编译和关键流程操作；真机验证仅在实际执行后报告。

通过前两类新疆测试不代表外部票价、道路、班次或预约条件已核实。

## 13. M0-M3 交付顺序

### M0 基础

创建可导入工程、领域模型、Repository 契约、隔离演示模式、五入口骨架和核心计算测试。完成标志是工程可启动、数据可录入保存、测试可运行。

### M1 行程

实现旅行、参与人、分组、车辆、日期、活动、交通、停车、住宿、预订条件和待办的完整单机持久化流程。

### M2 费用

实现预算区间、未知费用、房晚和公共费用分摊、待确认与已确认账单、退款、转账和剩余结算。

### M3 联动协作

实现冲突检查、调整预览、方案切换、revision 冲突、分享权限过滤、基础优惠比较和归档审计。云环境缺失时，真实登录、真实分享链接和跨设备同步保持“未接通”，但接口、云端代码和本机权限演示仍可测试。

## 14. 完成判定

每阶段报告可运行方式、自动测试、微信开发者工具验证、已知限制和下一阶段。最终需求映射逐项指向实现文件和测试。页面骨架、固定提示或单元测试通过不能单独视为完整首版。

正式完成仍需：全部自动测试通过、项目构建通过、微信开发者工具关键流程验证、需求矩阵逐项核对。真机、真实登录、真实分享和地图能力只在对应环境实际验证后标记完成。
