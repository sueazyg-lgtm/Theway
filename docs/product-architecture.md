# 首发产品架构

旅行列表负责创建、选择和重新打开旅行。进入单趟旅行后，五个业务入口共用当前旅行标识，不在每个入口创建独立数据副本。

| 入口 | 职责 |
| --- | --- |
| 旅行总览 | 基础信息、路线摘要、预算摘要、预订进度、重要待办 |
| 行程 | 按天展示地点、交通、游玩、住宿；编辑、排序与时间安排 |
| 预算 | 分类预算、实际账本、公共均分、按房间或指定人员分摊 |
| 预订与待办 | 购买、预约和核验状态分别保存 |
| 方案调整 | 活动启用或放弃、备选方案备注 |

页面通过应用服务执行操作；应用服务进行校验、版本冲突检查和持久化；领域计算保持独立。TypeScript 源码构建到 runtime 供微信页面加载。

当前已拆为旅行列表页与五个独立业务页面：`pages/overview`、`pages/itinerary`、`pages/budget`、`pages/bookings`、`pages/adjustments`。五页通过 `adapters/runtime.js` 共用同一 `TripService` 与本地存储，并通过 `components/trip-tabbar` 导航，因此页面切换不会复制行程数据。纯 TypeScript 的 `services/view-models.ts` 负责把同一聚合模型转换为页面视图。

真实分享需要服务端身份和访问授权，不能使用本地行程 ID 直接充当授权凭证。个人费用和订单详情默认不进入分享视图。当前云函数代码存在但环境未配置、未部署、未实际鉴权验证，因此产品仍显示为本机演示模式。

发布验收按 release-gates.md 执行；本地测试通过不代表真机、体验版或审核完成。
