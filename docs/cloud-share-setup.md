# 云端只读分享接通步骤

当前代码默认使用本机演示模式。接通真实分享前：

1. 在微信开发者工具中开通云开发环境，复制环境ID。
2. 在 `config/runtime.js` 中填写 `cloudEnvId`，并将 `cloudEnabled` 改为 `true`。
3. 在开发者工具中依次上传并部署 `share-create`、`share-read`、`share-revoke` 三个云函数，选择云端安装依赖。
4. 建立 `share_grants` 集合。数据库权限设为禁止客户端直接读写，所有访问只经云函数。
5. 在真机使用两个不同微信身份测试创建、读取、过期和撤销。

分享授权默认7天有效。接收页只读取云端已经脱敏的公共视图。分享ID属于访问凭证，不应记录到日志或分析事件。拥有者OpenID由云函数上下文取得，不接受客户端传入身份。

未完成上述步骤时，页面只提供本机分享预览，并明确提示真实分享未接通。
