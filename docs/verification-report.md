# 验证报告

更新时间：2026-09-10 10:31（Asia/Shanghai）

## 已验证

| 范围 | 实际结果 | 证据 |
|---|---:|---|
| 全量自动化 | 16 个测试文件、75 个测试通过 | `npm test` |
| 核心服务流程 | 连续 10/10 次成功 | `tests/core-flow.test.ts` |
| 五入口架构 | 旅行列表 + 总览/行程/预算/预订/调整 5 页通过 | `tests/page-architecture.test.ts` |
| 共享视图模型 | 3/3 通过 | `tests/view-models.test.ts` |
| 类型与构建 | 0 错误 | `npm run typecheck`、`npm run build` |
| 项目结构 | 7 个页面、10 个关键运行文件、3 个云函数 | `npm run validate` |
| JavaScript 语法 | 36/36 通过 | `node --check` |
| 退款与归档 | 退款上限/确认语义、归档恢复与数据保留通过 | `tests/trip-service.test.ts` |
| 实际结算 | 转账先登记为待确认，确认后才改变余额和后续建议 | `tests/trip-service.test.ts`、`tests/view-models.test.ts` |
| 错误提示 | 内部错误码均转为可理解的中文提示 | `tests/error-messages.test.ts`、`tests/page-architecture.test.ts` |
| 本地保存反馈 | 最后一次成功写入时间可读取并在总览展示 | `tests/storage.test.ts`、`tests/page-architecture.test.ts` |

核心流程每轮覆盖创建旅行、添加及排序活动、预算分摊、预订状态、费用幂等提交、确认实付、重新实例化仓储读取数据，以及生成不含个人账本字段的只读视图。每轮校验分摊叶子金额之和等于费用总额，重复请求不生成第二条账单。结算转账另外验证“待确认不改变余额、已确认才改变余额”。

新疆案例自动化已核对 10 天、9 晚、17 条逐房晚记录、住宿总额 11,309.68 元、已购买票务 1,005.00 元、已确认实际费用 0 元、C01—C10 冲突，以及已预订/已购买不会自动产生实付。

## 本轮环境检查

- 本机已安装微信开发者工具 `2.02.2608070`，自带 CLI 可读取。
- CLI 支持打开、预览和上传，但没有单独的“仅本地编译”命令。
- 微信工具随包提供的 `wechatide-skill 0.3.9` 已读取；其首次运行诊断返回 `cli_unavailable`，具体原因为内部命令超时。按工具自身门禁已停止继续调用，不把 CLI 文件存在当成编译成功。
- 本会话的 Windows 桌面控制运行接口未暴露，因此没有冒充完成开发者工具实时编译。

## 尚未验证或接通

- 本轮代码在微信开发者工具中的实时编译与五页逐项点击。
- 两台不同系统真机测试。
- 3—5 名未参与开发者独立完成创建、添加行程及查看分享。
- 云环境部署、真实登录、跨设备分享、撤销授权和越权访问。
- 体验版上传、体验版回归与审核前检查。
- 完整方案变更预览仍属待开发范围；首版要求的活动启用/放弃和备选备注已实现。

本地自动化、结构检查或 CLI 可用均不能替代上述真实验收。
