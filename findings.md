# 发现与决策

## Requirements
- 读取 V0.2 需求和 `D:\life\新疆自驾预算攻略计划.md`。
- 先检查仓库并记录技术选型和需求映射，再按 M0-M3 分阶段实现。
- 第 14-16 章按具体适用范围覆盖较早的一般规则；第 10 章虚构测试保留。
- 新疆案例转成标记清楚的种子数据和冲突清单。
- 已预订不等于实付；旧方案文字不自动确认。
- 不写死三人、新疆或自驾。
- 优先持久化行程、预算、分摊、实际账本及结算；核心计算独立测试。
- 配置缺失时提供手动录入或隔离演示模式；如实列出未接通能力。
- 验证通用测试与案例验收矩阵，报告实际结果。
- 正式名称和视觉参考后补；当前选择须可逆并记录。

## Research Findings
- 当前工作区仅含 `.git`，没有提交，也没有既有代码或规划文件。
- 新疆历史经验提示原路线与 Plan B 必须分开，晚段日期与景区交通可行性存在待核验风险；最终以本轮实际文档为准。
- V0.2 定义五个旅行内入口：总览、行程、预算、预订与待办、方案调整；旅行列表是进入具体旅行前的页面。
- 阶段口径：M0=结构/模型/演示数据/页面骨架/计算模块；M1=基础设置至预订待办；M2=预算、实际账单、退款、分摊、结算；M3=冲突、预览、方案、只读分享、版本冲突、优惠、归档。
- 财务硬约束：金额用人民币分；预算与实付独立；待确认账单不进实际净支出；预订/已购状态不生成实付；退款关联原账单且不超额；结算转账不算消费；分摊与余额必须守恒。
- M3 的正式多人分享要求服务端持久化和鉴权；本地存储只能作为隔离演示/缓存，不能冒充跨设备共享。
- 外部配置缺失时，地点、路线耗时、价格、预订条件均允许人工录入；不能绘制虚假道路或伪造成功提示。
- V0.2 第14-16章已给出新疆 seed、C01-C10 冲突和 X01-X20 验收矩阵，并明确它们不替代通用规则。
- 案例预算基线：住宿 11309.68 元；门票计划 1905 元、原文已购 1005 元但无正式支付流水；已知预算 19561.68-20972.68 元且不含餐饮与未明确停车费。
- 案例补充模型：两级分摊、ParkingSession、区间耗时/窗口、预订资格条件、费用包含状态、结构化建议规则、ImportRecord 及冲突组。
- 已完整读取原攻略 489 行。其正文把当前线路标为“可行/可执行”，但 V0.2 已明确这些旧判断只能作为来源说明，系统需独立输出“信息不足/有风险”。
- 原攻略住宿状态并不一致：多晚“已预订/已确认”，禾木为待确认，铁热克提只给房价未说明状态；所有状态必须逐条保留且均不得推导为已支付。
- 原攻略 9 月 29 日当前主线是出园到贾登峪取车后自驾白哈巴；同文后部“完成白哈巴往返后出园”的班车叙述属于旧方案冲突。
- 原攻略同时保留当前后半程、原后半程、天气差/道路不稳替代线，符合 PlanVersion，而非覆盖单一路线。
- 原攻略中的电话、营业时间、票价、道路与预约等均是案例来源陈述；本轮不进行外部事实复核，也不把它们升级为当前确认事实。

## Technical Decisions
| Decision | Rationale |
|----------|-----------|
| 推荐微信原生小程序 + TypeScript | 与需求建议一致，空仓库下依赖和跨端抽象最少 |
| 案例作为独立 seed/fixture | 避免把新疆、人数和自驾渗入领域模型 |
| 核心计算以纯 TypeScript 模块实现 | V0.2 明确要求时间、费用、冲突、优惠、结算脱离页面独立测试 |
| 金额内部统一整数分 | 避免浮点误差并满足分摊余数及守恒规则 |
| Repository 接口隔离 demo-local 与 cloud | 无配置可运行；有云环境后替换数据层而不重写页面和领域计算 |
| Vitest 运行核心测试 | 适合纯 TypeScript 快速红绿循环，避免依赖微信运行时 |
| 在当前 master 工作区直接开发 | 用户明确要求继续写代码，且当前目录将作为微信开发者工具导入根目录；空仓库无并行用户改动需要隔离 |

## 当前工具核验
- 本机 Node.js `v24.16.0`、npm `11.13.0`。
- Vitest 官方迁移文档说明 5.0 需要 Node.js 22.12 或更高；当前 Node 版本满足。
- TypeScript 官方文档确认 `tsconfig.json` 定义 TypeScript 项目根与编译选项；领域测试仍需单独运行 `tsc --noEmit`，因为 Vitest 转换 TypeScript 但不负责完整类型检查。
- npm 注册表当前返回：Vitest `5.0.0`、TypeScript `7.0.2`、`miniprogram-api-typings` `5.2.3`。
- 微信官方开发文档 URL 本轮无法由联网读取器打开，因此不把配置细节称为在线文档核验结果；后续以项目配置静态检查和本机微信开发者工具导入编译为实际证据。

## 十三模块映射口径
1. 旅行建立与基础设置（Trip）
2. 参与人、分组与车辆（Participant/Group/Vehicle）
3. 路线与旅行日（PlanVersion/TripDay）
4. 每日活动与时间线（Activity）
5. 交通、地点、停车与补给（TransportLeg/Place/ParkingSession）
6. 逐晚住宿与房间（Lodging/RoomNight）
7. 预订、出行条件与待办（Booking/TravelCondition/Task）
8. 预算与优惠比较（BudgetItem/DiscountOption）
9. 实际账本与退款（Expense/ExpenseLine/Refund）
10. 分摊与结算（分摊快照/SettlementTransfer）
11. 冲突检查、变更预览与方案切换（ChangePreview/PlanVersion）
12. 分享、权限与版本冲突（TripAccess/ShareGrant/revision）
13. 归档、审计与来源追溯（AuditLog/InformationSource/ImportRecord）

## 五入口映射
- 旅行总览：模块 1-3、总览聚合、分享及归档入口。
- 行程：模块 3-6。
- 预算：模块 8-10。
- 预订与待办：模块 7。
- 方案调整：模块 11，并联动模块 3-10；模块 12-13 为跨入口能力。

## Issues Encountered
| Issue | Resolution |
|-------|------------|
| 仓库为空，无法沿用既有工程模式 | 将在需求梳理后比较 2-3 个从零起步方案 |
| 文档声称十三个模块但未提供单独编号清单 | 依据完整流程与页面职责形成显式模块映射，并在设计中列明口径 |
| npm 用户缓存目录被沙箱限制 | 受控执行 `npm view` 后获得版本号 |
| 微信官方页面联网读取失败 | 不重复同一路径，改用本机开发者工具实际验证 |

## Resources
- `C:\Users\luoji\.codex\attachments\8b187318-2586-4c6d-92e7-6b8423390915\pasted-text.txt`
  - `D:\life\新疆自驾预算攻略计划.md`

## Visual/Browser Findings
- 暂无。
